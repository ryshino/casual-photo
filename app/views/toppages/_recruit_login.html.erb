<section class="hero">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        【採用担当者様　専用ログインフォーム】
      </h1>
      <h4>
        下記の「専用ログイン」ボタンを押していただければ、<br>すぐに専用アカウントでログインが可能です。
      </h4>
    </div>
  </div>
</section>

<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-5">
        <%= form_with(url: login_path, scope: :session, local: true) do |f| %>
        
          <div class="field">
            <%= f.label :email, "メールアドレス", class: "label" %>
            <%= f.email_field :email, value: 'recruit@example.com', class: "input" %>
          </div>        

          <div class="field">
            <%= f.label :password, 'パスワード', class: "label" %>
            <%= f.password_field :password, value: 'password', class: "input" %>
          </div>
          
          <%= f.submit '専用ログイン', class: "button is-success mt-2" %>
        <% end %>
      </div>
    </div>
  </div>
  <div>
    <p>＜採用担当者様　専用アカウント＞</p>
    <p>メールアドレス：recruit@example.com</p>
    <p>パスワード：password</p>
    <p>※フォームに予め入力済</p>
  </div>
</section>